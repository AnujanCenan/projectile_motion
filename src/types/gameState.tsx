// type GameState = [angle, velocity, yPosScalar, xScroll];

// type angle = number;
// type velocity = number;
// type yPosScalar = number;
// type xScroll = number;

type GameState = {
  angle: number,
  velocity: number,
  yPosScalar: number,
  xScroll: number
}